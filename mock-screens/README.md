# Cup Mock Screens

Small Expo Go playground for trying screen designs without touching the main app.

## Run

```sh
cd mock-screens
npm run start
```

Then scan the QR code with Expo Go.

This mock is intentionally standalone: it has no NFC plugins, native project folders, or production navigation.
